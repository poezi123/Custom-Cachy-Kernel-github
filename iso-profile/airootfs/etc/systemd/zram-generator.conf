# zram statt Swap-Partition: komprimierter RAM-Swap.
# Auf einem Compile-Laptop deutlich besser als Disk-Swap - kein NVMe-Verschleiss,
# keine IO-Stalls wenn ein grosser Build den RAM fuellt.
[zram0]
zram-size = min(ram, 16384)
compression-algorithm = zstd
swap-priority = 100
fs-type = swap
